type AuthPayload {
    token: String!
    user: User!
}

type LogoutResponse {
    message: String!
}

extend type Query {
    me: User! @auth(guards: ["api"])
}

type Mutation {
    #    Authentication
    login(
        email: String!
        password: String!
    ): AuthPayload!
    logout: LogoutResponse
}
