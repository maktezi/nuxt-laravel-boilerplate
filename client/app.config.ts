export default defineAppConfig({
    ui: {
        colors: {
            neutral: 'slate',
            primary: 'green'

        },
        container: {
            base: 'max-w-[100rem] w-full'
        }
    }
})
